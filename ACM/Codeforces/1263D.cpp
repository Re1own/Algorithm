#include <iostream>

using namespace std;

const int maxn = 1e5 + 5;
int pre[100];
int father[maxn];

char find_father(char ch) {
	
}

void union()
int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(0);
	int n;
	cin >> n;
	int flag = 0, f = 0;
	for (int i = 0; i < n; i++) {
		string str;
		cin >> str;
	}
	cout << f + flag << endl;
	return 0;
}
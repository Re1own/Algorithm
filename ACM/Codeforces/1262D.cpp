#include <iostream>
#include <vector>

using namespace std;

typedef long long ll;

void BUFF(void) {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
}

int main(void) {
	BUFF();
	
	return 0;
}
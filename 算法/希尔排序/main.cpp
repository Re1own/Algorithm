#include <iostream>
#include <sstream>
#include <string>
#include <string.h>
using namespace std;

int main()
{
    string a;
    getline(cin,a);
    int len = a.length();
    cout << len;
    return 0;
}
